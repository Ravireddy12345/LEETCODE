import random
import time 
name=input("Enter the player 1 name:")
name=input("Enter the player 2 name:")
nums=[]
while(len(nums)<5):
   d=random.randint(1,10)
   if(d in nums):
      continue
   else:
      nums.append(d)
print(nums)
